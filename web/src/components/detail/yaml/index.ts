// Barrel export for the inline YAML editor module. Keeps consumer
// imports short (`./yaml` instead of `./yaml/YamlEditor`).

export { YamlEditor } from "./YamlEditor";
export { EditButton } from "./EditButton";
