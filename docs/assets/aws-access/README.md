# AWS Access screenshots

Placeholder directory for the screenshots referenced from
`docs/usage/aws-access.md` and the v1.1 launch blog post.

Populate with:

- `forward-view-pod.png` — Pod detail's AWS access tab on a role with
  attached managed policies + IRSA-only binding.
- `forward-view-dual-source.png` — Same tab on a SA with dual-source
  bindings showing the `DUAL_SOURCE_IRSA_SHADOWED` warning.
- `reverse-lookup.png` — Page with a populated result table after a
  `iam:PassRole` chip click.
- `locked-pane-rbac.png` — Locked-feature pane rendering a
  `RBAC_DENIED` reason with a Missing list.
